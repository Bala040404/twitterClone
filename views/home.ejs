<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/home.css" rel="stylesheet">
    <title>Document</title>
</head>

<body>

    <div id="full">
        <div id="left">
            <h1>twitter</h1>


        </div>
        <div id="mid">
            <% for (let t of tweets){ %>

                <h2>
                    <img id="dp" src="<%= t.user.picture %>">
                    <%= t.user.username %>
                </h2>
                <h3>

                    <%= t.tweet %>
                </h3>
                <img id="pic" src="<%= t.img %>">


                <% if(t.user.equals(req.user._id)){%>

                    <form action="/twitter/edit/<%= t._id %>">

                        <button>edit</button>
                    </form>



                    <% } %>


                        <% } %>

        </div>

        <div id="right">

            <form id="find" action="/twitter/find">
                <button>find people</button>
            </form>

            <form id="new" action="/twitter/new">
                <button>tweet</button>
            </form>



        </div>


    </div>
</body>

</html>